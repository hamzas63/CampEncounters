<h1>Editing User</h1>

<%= render 'form', user: @user %>

<%= link_to 'Show', @user %> |

<% if !@user.admin? %>
  <%= link_to 'Back', users_path %>
<% end %>
<% if @user.admin? %>
  <%= link_to 'Back', admin_users_path %>
<% end %>
